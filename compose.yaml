services:
  jupyter:
    image: dannb/jupyter-testing
    container_name: jupyter-testing
    user: worker
    restart: unless-stopped
    volumes:
    - ./jupyter-data/:/home/worker/data
    environment:
     JUPYTER_TOKEN: "JupyterInDocker"
    ports:
    - 127.0.0.1:8888:8888